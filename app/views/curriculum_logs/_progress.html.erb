<h1 class ="text-center mt-5"><%= current_user.profile.name %>'s loguma</h1>
  <div class ="text-center">
  <p>today's total: <%= daily_study_time(current_user) %></p>
  </div>
<!-- 進捗バー -->
  <div class="col-6 mx-auto progress" role="progressbar" aria-label="進捗バー" aria-valuemin="0" aria-valuemax="1000">
    <div class="progress-bar progress-bar-striped bg-success progress-bar-animated" style="width: <%= (current_user.hours_with_minutes[0] / 1000.0) * 100 %>%;">
      <%= (current_user.hours_with_minutes[0] / 1000.0 * 100).to_i %>%
    </div>
  </div>

<div class="row mt-1">    
  <div class="text-center my-3">     
      <%= link_to profile_path do %>
        <%= image_tag current_user.profile.avatar.url, class: 'rounded-circle mr15', width: '100', height: '100' %> 
      <% end %>
  </div>
</div>
