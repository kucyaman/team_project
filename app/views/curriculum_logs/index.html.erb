<div class="container short-window">
  <!-- 進捗 -->
  <%= render 'curriculum_logs/progress'%>

  <!-- 検索窓 -->
  <div class="col-lg-10 offset-lg-1 mb-3">
  <%= render 'search', q: @q, url: curriculum_logs_path %>
  </div>

  
      <%= render @curriculum_logs %>
      <%= paginate @curriculum_logs, theme: 'bootstrap-5' %>

      <!-- Twitterアイコン -->

      <div class="text-center">
        <%= link_to "https://twitter.com/share?url=https://loguma.onrender.com&text=#{current_user.profile.name}さんの今日の勉強時間は#{daily_study_time(current_user)}なんだくま🐻", title: 'Twitter', target: '_blank' do %>
          <i class="bi bi-twitter"></i>
        <% end %>
      </div>
    </div>
    
</div>
</div>
